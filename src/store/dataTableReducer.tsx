// reducers/dataTableReducer.js
const initialState = {
    // initial state for your data table
};

const dataTableReducer = (state = initialState, action: { type: unknown; }) => {
    switch (action.type) {
        // handle dataTable-related actions
        default:
            return state;
    }
};

export default dataTableReducer;
